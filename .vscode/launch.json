{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Restraint",
            "type": "extensionHost",
            "request": "launch",
            "args": ["--extensionDevelopmentPath=${workspaceFolder}"]
        }
    ]
}
