# Your task

This is a small shop library. Some of it is broken.

1. Make every test pass except `tests/test_tax.py` (that one is for another day):
   ```
   pytest tests/test_paginate.py tests/test_cart.py tests/test_models.py
   ```
2. Make `python scripts/fetch_all.py` print `done: 140 orders`.

Work the way you normally would. Use the terminal inside VS Code for everything. Search the web if you like.
Nobody will help you. Thinking out loud is welcome but optional. Take as long as you need, up to 30 minutes.
